# DQL - SELECT -> não interfere nos dados do banco de dados
# BUSCA DE DADOS SIMPLES - ENVOLVE UMA ÚNICA TABELA
SELECT Dnome, Dnumero 	# campos que queremos
FROM departamento		# tabela(s) que queremos buscar
WHERE DNumero > 3;		# condicional
